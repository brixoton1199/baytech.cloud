# Cloudflare Pages Configuration
# This file documents the expected configuration
# Actual settings are managed in Cloudflare Dashboard

[build]
output_dir = "baytech.cloud"

[deployment]
production_branch = "main"

